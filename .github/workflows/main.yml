name: Fudma_Dev_Action
on:
  push:
    branches:
      - main
jobs:
  build:
    name: build
    runs-on: self-host
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install packages
        run: npm install
      - name: Run linter
        run: npm run lint
